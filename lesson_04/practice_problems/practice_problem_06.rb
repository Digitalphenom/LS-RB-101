['ant', 'bear', 'caterpillar'].pop.size

# ---wrong solution---
# pop destructively removes the last element in the collection.
# size will return the size or length of the array which is 2.

# in this case, size evaluates the return value of pop, not the collection, the answer is actually => 11.