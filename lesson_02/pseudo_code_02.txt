Given a set of names combine them together and print the result

- define a method with one parameter that represents an array
- access the first name using index assignment (parameter[0])
- access the second name using index assignment (parameter[1])
- within the code block concatenate both parameters

combine and Print to screen


START

# Given a variable with an array set of values

SET name = [sedrick, penate]

combine_name(name)
  name[0] + name[1]
end

PRINT combine_name

END
