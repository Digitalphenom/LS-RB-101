Given a set of two integers

Return the value of adding both integers
- create a method that takes two parameters
- use parameter num1,num2 and the addition symbol(+) to produce the 
 - basic math expression (num1 + num2)
- call the method and supply two integers

START

add_two_integers(num1,num2)
  num1 + num2
end

add_two_integers(5, 6)

END
