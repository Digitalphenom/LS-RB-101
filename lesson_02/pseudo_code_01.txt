Given a set of two integers
Return the value of adding both integers

- define a method with two parameters named num1 and num2
- within the methods code block, write the expression (num1 + num2)
- call the method and pass two arguments

START

add_two_integers(num1,num2)
  num1 + num2
end

add_two_integers(5, 6)

END
